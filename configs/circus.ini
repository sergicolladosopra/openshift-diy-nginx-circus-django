[socket:dwebapp]
host = $OPENSHIFT_DIY_IP
port = 15005

[watcher:dwebworker]
cmd = chaussette --fd $(circus.sockets.dwebapp) $APP_NAME.wsgi:application
use_sockets = True
numprocesses = 2

[env:dwebworker]
PYTHONPATH = $OPENSHIFT_REPO_DIR/$APP_NAME

