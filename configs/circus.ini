[socket:dwebapp]
host = $OPENSHIFT_DIY_IP
port = 15005

[watcher:dwebworker]
cmd = $OPENSHIFT_DATA_DIR/virtualenv/bin/chaussette --backend gevent --fd $(circus.sockets.$APP_NAME) $APP_NAME.wsgi:application
use_sockets = True
numprocesses = 2

[env:dwebworker]
PYTHONPATH = $OPENSHIFT_REPO_DIR/$APP_NAME

